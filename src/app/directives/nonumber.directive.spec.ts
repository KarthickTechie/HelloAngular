import { NonumberDirective } from './nonumber.directive';

describe('NonumberDirective', () => {
  it('should create an instance', () => {
    const directive = new NonumberDirective();
    expect(directive).toBeTruthy();
  });
});
