import { QuickdeleteDirective } from './quickdelete.directive';

describe('QuickdeleteDirective', () => {
  it('should create an instance', () => {
    const directive = new QuickdeleteDirective();
    expect(directive).toBeTruthy();
  });
});
