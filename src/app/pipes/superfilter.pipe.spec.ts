import { SuperfilterPipe } from './superfilter.pipe';

describe('SuperfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SuperfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
